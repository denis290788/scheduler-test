export interface Shift {
    employee: string;
    store: string;
    role: string;
    planStart: string;
    planEnd: string;
}

export interface FactShift {
    employee: string;
    store: string;
    role: string;
    factStart: string;
    factEnd: string;
}
